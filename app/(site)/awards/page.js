import PageClient from './PageClient';

export const metadata = {
  title: 'Honors & Awards',
  description: 'Honors, awards and distinguished appearances of Ashwani Kumar, IAS.',
  alternates: { canonical: '/awards' }
};

export default function Page() {
  return <PageClient />;
}
